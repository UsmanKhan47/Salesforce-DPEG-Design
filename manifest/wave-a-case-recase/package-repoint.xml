<?xml version="1.0" encoding="UTF-8"?>
<!--
  ARCHITECTURE.md §1 Wave A — case-only re-case, REPOINT step (deploy 3 of 3).

  ⚠️ SUPERSEDED BY THE SPLIT BELOW — DO NOT DEPLOY THIS FILE AS-IS.
  The FlexiPage member here belongs to the BLOCKED Underwriting__c field (see
  destructiveChangesPost.xml's header) — bundling it with the Report member
  would fail deploy since the Underwriting field is never touched in the
  clean sub-wave. This file documents the FULL 4-field target state only.
  For an EXECUTABLE sequence today, use package-repoint-clean.xml (Report only).

  STATUS: PREPARED, NOT AUTHORISED. Run ONLY after deploy 2
  (package-create.xml) has committed — these readers require the new-cased
  field to actually exist and resolve live in the org:
    - Report columns/groupingsDown <field> resolve case-insensitively as loose
      string references (verified safe to re-case any time), so this one
      would technically validate even before deploy 2 — but is bundled here
      for a single, simple, always-safe-order sequence.
    - The FlexiPage spanning fieldItem
      (Record.Primary_Underwriting__r.Cash_On_Cash_Return__c) is NOT
      forgiving — it requires an exact/live match against the field as
      currently defined in the org. Submitted before deploy 2 commits, this
      FAILS deploy with "Something went wrong. We couldn't retrieve or load
      the information on the field: ...Cash_On_Cash_Return__c." (verified
      2026-07-17, see case-only-field-recase-noop.md memory).

  Local source files were already edited in the same pass as the field/FLS
  changes (STEP 2 "build metadata" is one editing pass; this is a separate
  DEPLOY step only because of the live-resolution ordering constraint above).

  Deploy (only once deploy 2 has committed):
    sf project deploy start \
      --manifest manifest/wave-a-case-recase/package-repoint.xml \
      --target-org DPEG-Acq-5 \
      --test-level RunLocalTests --wait 30
  -->
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <types>
        <members>Dispositions/Avg_Days_on_Market</members>
        <name>Report</name>
    </types>
    <types>
        <members>Opportunity_Record_Page</members>
        <name>FlexiPage</name>
    </types>
    <version>67.0</version>
</Package>
