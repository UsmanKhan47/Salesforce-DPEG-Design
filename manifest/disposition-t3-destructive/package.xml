<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!--
    Companion EMPTY manifest for the disposition-t3-destructive post-destructive
    deploy. A post destructive changes deploy still requires a manifest flag, and
    this one must be empty: everything additive for Tranche 3 ships in the main
    payload, and this deploy exists only to remove the one component listed in
    destructiveChangesPost.xml alongside it. Same shape as
    manifest/disposition-redesign-destructive/package.xml.
    -->
    <version>67.0</version>
</Package>
