<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!--
    Retires the stock, never-finished Salesforce self-registration boilerplate
    cluster: MicrobatchSelfRegController, its test, and the Visualforce page the
    controller serves.

    STATUS: deletion APPROVED by the user (2026-08-17). The DEPLOY itself is not
    self-authorising: it still goes through the devops agent and the standing
    deployment confirmation gate. This file only DESCRIBES a deletion; it performs
    nothing on its own.

    WHY THIS EXISTS AT ALL (the local file deletion is NOT sufficient):
    All three components are LIVE in usman.dpeg. Verified 2026-08-17 by Tooling
    API query:
      - ApexClass MicrobatchSelfRegController     01piw000000VCJYAA4
      - ApexClass MicrobatchSelfRegControllerTest 01piw000000VCJZAA4
      - ApexPage  MicrobatchSelfReg               066iw0000004oXAAAY
    A source deploy is additive. Removing the files from force-app stops them
    being redeployed, but it does NOT un-deploy what is already in the org, and
    RunLocalTests runs the ORG's test classes. Until this manifest is deployed,
    MicrobatchSelfRegControllerTest.testMicrobatchSelfRegController keeps failing
    and keeps blocking every RunLocalTests deploy.

    WHY IT IS SAFE TO DELETE (dependency sweep, 2026-08-17):
      1. Org dependency graph. MetadataComponentDependency queried against all
         three component Ids returns exactly TWO rows, and both are INTERNAL to
         this cluster (the test references the controller; the page references the
         controller). Nothing outside the cluster references any of the three.
      2. The Experience Cloud angle raised at design time is moot: Communities is
         not enabled in this org at all. "sf org list metadata" for type Network
         returns INVALID_TYPE "Cannot use: Network in this organization", for type
         CustomSite returns an empty list, and SOQL against Site returns 0 rows.
         The force.ignored networks/ and sites/ files in this repo are a snapshot
         from an earlier org, not a description of usman.dpeg.
      3. Even in that stale snapshot the page is unreferenced: the site's
         selfRegPage is CommunitiesSelfReg, and every other page slot
         (authorizationRequired, bandwidthExceeded, fileNotFound, genericError,
         inMaintenance, index) names a different page. The network file sets
         selfRegistration false, status UnderConstruction, and routes every page
         override through Designer or Standard, never through Visualforce.
      4. Repo sweep. The only surviving references are in force.ignored profiles
         and in the .sf source.tracking cache. Profiles never deploy from this
         repo (force-app/main/default/profiles/** is force.ignored), so the stale
         classAccess / pageAccess entries in ~40 profiles cannot fail this deploy;
         the org prunes its own profile grants when a class or page is deleted.
         This matches the established repo precedent for stale profile entries
         recorded in manifest/portfolio-deal-a3/destructiveChangesPost.xml.
      5. The controller's only OUTBOUND reference is System.Page.
         CommunitiesSelfRegConfirm, which survives untouched.

    NOT IN SCOPE. Do not widen this manifest:
      - CommunitiesSelfRegController / CommunitiesSelfReg / CommunitiesSelfRegConfirm
        and the other roughly 9 stock Site and Communities boilerplate classes.
        Their SeeAllData=true violation of ARCHITECTURE.md section 2 was flagged at
        design time and explicitly left unauthorised. CommunitiesSelfRegController
        also PASSES its test (it calls Site.createExternalUser, whose exception type
        its catch block actually matches), so it is not blocking anything.

    ORDERING: the page depends on the controller, so both are removed in this one
    manifest and the Metadata API resolves the intra.transaction order. Post
    destructive (not pre) so that, if devops bundles this with any constructive
    metadata, the deletion lands after that metadata settles.

    DEPLOY: usman.dpeg is a production org, so the deletion must run with a test
    level; RunLocalTests is correct here, and the class being deleted is the one
    currently failing it. Run a validation only dry run first, per standing
    project practice. The exact command is deliberately NOT reproduced in this
    comment: an XML comment cannot legally contain a double hyphen, and every sf
    CLI flag begins with one. Same reasoning as
    manifest/permset-retirement/1-acquisitions-dashboard-access/destructiveChanges.xml.

    ROLLBACK: recover the source from git history (this commit) and redeploy. The
    class is stock Salesforce boilerplate and is also regenerated by Salesforce if
    Experience Cloud self.registration is ever switched on, so a true rollback is
    unlikely to be needed.
    -->
    <types>
        <members>MicrobatchSelfReg</members>
        <name>ApexPage</name>
    </types>
    <types>
        <members>MicrobatchSelfRegControllerTest</members>
        <members>MicrobatchSelfRegController</members>
        <name>ApexClass</name>
    </types>
    <version>67.0</version>
</Package>
