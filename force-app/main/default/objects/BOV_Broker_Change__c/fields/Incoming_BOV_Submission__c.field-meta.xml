<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!--
        THIS COMMENT SITS INSIDE THE ROOT ELEMENT DELIBERATELY - see Disposition__c.field-meta.xml
        in this folder for why a comment above the root breaks `sf` deploy.

        Twin of Outgoing_BOV_Submission__c in this same folder; read that file's comment for the
        full "two lookups to one object" reasoning. relationshipName Incoming_Broker_Changes is
        distinct from Outgoing_BOV_Submission__c's Outgoing_Broker_Changes, and both are free on
        the BOV_Submission__c parent (no other field in this repo references BOV_Submission__c).
    -->
    <fullName>Incoming_BOV_Submission__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>The BOV Submission for the incoming replacement broker.</description>
    <inlineHelpText>The BOV response from the broker who replaced the outgoing one.</inlineHelpText>
    <label>Incoming BOV Submission</label>
    <referenceTo>BOV_Submission__c</referenceTo>
    <relationshipLabel>Incoming Broker Changes</relationshipLabel>
    <relationshipName>Incoming_Broker_Changes</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
