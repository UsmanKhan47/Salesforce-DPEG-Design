<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Transaction_Deal__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Links a fan-out checklist Task to its Transaction without using WhatId, so the checklist stays out of the standard Activity timeline.</description>
    <label>Transaction Deal</label>
    <referenceTo>Transaction__c</referenceTo>
    <relationshipLabel>Checklist Tasks</relationshipLabel>
    <relationshipName>Checklist_Tasks</relationshipName>
    <required>false</required>
    <type>Lookup</type>
</CustomField>
