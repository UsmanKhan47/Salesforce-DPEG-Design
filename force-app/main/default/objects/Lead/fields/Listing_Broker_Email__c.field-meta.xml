<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Listing_Broker_Email__c</fullName>
    <description>Email address of the listing broker, as stated in the broker email. This is a display field only — the claim engine reads Broker_First__c / the forwarding envelope, never this field.</description>
    <inlineHelpText>Populated by LLM extraction from the inbound broker email; null when not stated in the email.</inlineHelpText>
    <label>Listing Broker Email</label>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <type>Email</type>
</CustomField>
