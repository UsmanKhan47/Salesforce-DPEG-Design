<?xml version="1.0" encoding="UTF-8"?>
<CompactLayout xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Assigned to the Account `Investing_Entity` record type via that record type's own
         <compactLayoutAssignment>. A CompactLayout does NOT name the record type it serves;
         the binding lives on the RecordType, so both files must deploy together. Mirrors
         objects/Contact/compactLayouts/Investor_Compact_Layout on the Contact side. -->
    <fullName>Investing_Entity_Compact_Layout</fullName>
    <fields>Name</fields>
    <fields>Entity_Type__c</fields>
    <fields>Phone</fields>
    <label>Investing Entity Compact Layout</label>
</CompactLayout>
