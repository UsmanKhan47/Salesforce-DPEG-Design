<?xml version="1.0" encoding="UTF-8"?>
<CompactLayout xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Assigned to the Contact `Investor` record type via that record type's own
         <compactLayoutAssignment>. A CompactLayout does NOT name the record type it serves;
         the binding lives on the RecordType, so both files must deploy together. Mirrors
         objects/Lead/compactLayouts/Investor_Onboarding_Layout on the Lead side. -->
    <fullName>Investor_Compact_Layout</fullName>
    <fields>Name</fields>
    <label>Investor Compact Layout</label>
</CompactLayout>
