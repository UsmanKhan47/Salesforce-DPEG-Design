<?xml version="1.0" encoding="UTF-8"?>
<QuickAction xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- THIS NOTE LIVES IN A COMMENT INSIDE THE ROOT ELEMENT - a comment placed ABOVE
         <QuickAction> breaks `sf` deploy at source conversion. See
         Development_Feasibility_Review__c.Move_to_Feasibility_Analysis.quickAction-meta.xml for
         the full rationale behind this set of actions and for why there is no <description>
         element.

         THE TERMINAL HOP. Development_Feasibility_Review__c.Stage__c sequence is
         Requested -> Feasibility analysis -> Vendor proposals -> Share Opinion -> Completed. This
         action serves Share Opinion -> Completed, gated on
         Development_Feasibility_Review_Record_Page by
         {!Record.Stage__c} EQUAL 'Share Opinion'. 'Completed' is terminal - no button follows
         this one; the page's previous catch-all criterion (Stage__c NE 'Completed') correctly
         gives way to four exact-stage entries, one per hop, with nothing rendered at the
         terminal.

         ⚠ THIS FILE'S BODY IS IDENTICAL TO
         Construction_Feasibility_Review__c.Move_to_Completed.quickAction-meta.xml. Quick actions
         are object-scoped, so these are deliberately separate files sharing a label - not
         duplication to eliminate. -->
    <actionSubtype>Action</actionSubtype>
    <label>Completed</label>
    <lightningWebComponent>advanceRecordStage</lightningWebComponent>
    <optionsCreateFeedItem>false</optionsCreateFeedItem>
    <type>LightningWebComponent</type>
</QuickAction>
