<?xml version="1.0" encoding="UTF-8"?>
<LightningComponentBundle xmlns="http://soap.sforce.com/2006/04/metadata">
    <!--
        THE DESCRIPTION BELOW IS CAPPED AT 255 CHARACTERS AND ONLY A DEPLOY CATCHES A BREACH -
        Jest, the SLDS linter and code review have all passed a 258-character one in this repo.
        It was counted before this file was written; count it again before editing it.

        NOTE ALSO: this comment must sit INSIDE the root element. A comment placed ABOVE
        <LightningComponentBundle> breaks `sf` deploy at conversion with a misleading "unable to
        find matching parent xml file" error.

        isExposed=false: a LightningModal is opened from JS by c/meterRegister and
        c/onboardingChecklist, never dropped on a page, so it needs no target and must not
        appear in App Builder - where there would be no way to supply propertyAssetId.
    -->
    <apiVersion>67.0</apiVersion>
    <isExposed>false</isExposed>
    <masterLabel>Capture Meters</masterLabel>
    <description>UAT UT-001 meter capture grid: one row per space plus a whole-building row, saved against the property in a single transaction. Rows already on the register are pre-filled so a second capture edits rather than duplicates.</description>
</LightningComponentBundle>
