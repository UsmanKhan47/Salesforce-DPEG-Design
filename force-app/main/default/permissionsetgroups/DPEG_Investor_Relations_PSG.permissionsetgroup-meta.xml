<?xml version="1.0" encoding="UTF-8"?>
<PermissionSetGroup xmlns="http://soap.sforce.com/2006/04/metadata">
    <!--
        PermissionSetGroup.Description is capped at 255 characters. The full rationale below
        is security-relevant and was moved here rather than deleted; a comment is repo-only
        and is never sent to the Metadata API, so it can be as long as it needs to be. Do not
        paste this back into <description> verbatim - trim it if it needs to change there.

        Investor Relations persona: Edit on the IR-record-type Lead/Contact/Account
        population only, plus Investor Relations app and tab visibility. Deliberately
        carries NO acquisition, disposition, transaction or property-management access -
        the IR boundary is enforced by record type visibility here and by criteria-based
        sharing rules on Account and Lead.
    -->
    <description>Investor Relations persona: Edit on IR-record-type Lead/Contact/Account only, plus IR app/tab visibility. No acquisition, disposition, transaction or property-management access - boundary enforced by record type visibility and sharing rules.</description>
    <label>DPEG Investor Relations PSG</label>
    <permissionSets>DPEG_Base_Access</permissionSets>
    <permissionSets>DPEG_IR_Edit</permissionSets>
    <permissionSets>DPEG_App_Investor_Relations</permissionSets>
</PermissionSetGroup>
