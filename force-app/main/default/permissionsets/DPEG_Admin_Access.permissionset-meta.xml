<?xml version="1.0" encoding="UTF-8"?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <!--
        This comment must sit INSIDE the root element. A comment placed ABOVE <PermissionSet>
        breaks `sf` deploy at conversion with the misleading error "unable to find matching
        parent xml file". Broker_Protection_Access is the precedent for this pattern; it is
        used here because the rationale below is far longer than the 255-character cap on
        <description>.

        WHY Disposition__c.Wire_Verification_Completed__c IS GRANTED HERE (added 2026-08-09,
        code review W2). Metadata-API-deployed custom fields arrive with NO field permissions
        for ANY profile, System Administrator included - a trap this repo has already paid for
        (see ARCHITECTURE.md and the deal-driver gate incident, where a bare admin threw on the
        very field they had just deployed). The consequence here is worse than an admin simply
        not seeing a field: AN APPROVAL PAGE SILENTLY OMITS FIELDS THE APPROVER CANNOT READ.
        Wire_Verification_Completed__c is on Closing_Approval's approvalPageFields precisely so
        the approver sees the wire state when deciding (Gate 1, Q4), so without this grant an
        administrator acting as approver gets a Closing approval page with the wire state
        simply absent - no error, no blank, nothing. The design fails invisibly, which is the
        outcome Q4 existed to prevent.

        READ-ONLY on purpose: the field is DERIVED, maintained by the Wire_Verification_Rollup
        flow from the most recent Wire, and its own <description> says DO NOT HAND-EDIT. An
        editable grant would let an admin hand-set the anti-fraud gate the validation rule
        reads. Contrast Lease_Inquiry__c.OneDrive_URL__c below, which is editable because it is
        hand-entered.

        The two NAMED approvers do not depend on this set - they are covered by
        DPEG_Principal_PSG -> DPEG_Disposition_View, which already carries readable=true on the
        same field. This entry closes the ADMIN approver case only.

        POST-DEPLOY GATE (design section 8, gate A4): after deploying, open the Closing approval
        page as EACH persona that can approve - including an administrator - and confirm the
        Wire Verification Completed row is actually rendered. An admin smoke test alone proves
        nothing about the named approvers, and vice versa; FLS truth lives in the org, not in
        this repo, because profiles/** is .forceignore'd.

        WARNING: a PermissionSet deploy REPLACES this file's entire <fieldPermissions> set. Any
        org-side-only FLS grant not represented here is silently wiped by the next deploy of
        this file, even one made for an unrelated reason. Reconcile against the org before
        deploying; do not "keep it org-side so a redeploy can't disturb it".

        RECONCILED AGAINST THE ORG 2026-08-09 (Tranche 3B). This set, together with
        DPEG_Acquisition_Edit, DPEG_Acquisition_View and DPEG_Acquisitions, was retrieved from
        the default org and compared field-by-field, object-by-object, record-type-by-record-type
        against the repo copy: ZERO org-only grants were found in any of the four, so nothing
        in the org is at risk of being wiped by this deploy. Every difference ran the other way
        (repo ahead of org) and is the Tranche 2 / 3A / 3B work that has not been deployed yet.

        WHY LOI__c.Disposition__c IS GRANTED HERE (added 2026-08-09, Tranche 3B). Same reason as
        the Disposition__c fields above: Metadata-API-deployed custom fields arrive with NO field
        permissions for ANY profile, System Administrator included, so without this entry an
        administrator cannot see or set the lookup that distinguishes a sell-side LOI from a
        buy-side one. It is EDITABLE, unlike the derived counters above, because it is
        hand-entered (or pre-filled by a related-list New button) rather than maintained by a
        flow. The two new LOI record types are granted below for the same class of reason - a
        record type is invisible until it is granted, and Modify All Data is an OBJECT
        permission that confers no record-type access.

        ══════════════════════════════════════════════════════════════════════════════════
        2026-08-10 - PERMISSION SET CLEANUP. TWO CHANGES, AND THE SECOND IS A BUG FIX.
        ══════════════════════════════════════════════════════════════════════════════════

        (A) THE FOUR applicationVisibilities WERE MOVED HERE FROM Acquisition_App_Access
        (Gate 1, Q3). That set is being retired: its four app grants are 100% covered by
        DPEG_App_Acquisition / _Disposition / _Transaction / _PropertyMgmt for every PERSONA,
        but the administrator usman.khan.dpeg is in NO permission set group and holds NONE of
        those four sets, so Acquisition_App_Access was the only thing making the four Lightning
        apps visible to them. Because profiles/** is .forceignore'd, deleting it without this
        move would remove the admin's own App Launcher entries with nothing in source to restore
        them. Putting the grants HERE is what makes the restoration DEPLOYABLE - which is the
        stated purpose of this set - rather than four manual assignments repeated on every org
        rebuild. THE APPS MUST BE VISIBLE HERE AND VERIFIED BEFORE Acquisition_App_Access IS
        UNASSIGNED. Never the other way round.

        (B) 🔴 SIX recordTypeVisibilities WERE ADDED TO STOP THIS FILE SILENTLY WIPING THEM.
        Account.Broker_Firm, Account.Investor_Entity, Contact.Broker, Contact.Investor,
        Lead.Acquisition_Broker and Lead.IR_Investor were LIVE IN THE ORG on this set and were
        ABSENT FROM THIS FILE. A PermissionSet deploy REPLACES the whole grant list rather than
        merging into it, so the very next deploy of this file - including the app move in (A),
        made for an entirely unrelated reason - would have destroyed all six with no error and no
        warning. That is the identical failure that took down Broker Protection twice
        (2026-08-05 and 2026-08-06). Measured by retrieving this set from usman-dpeg into a
        scratch directory and diffing normalized grant lists: org 59 grants, repo 53, six
        org-only, zero repo-only.

        ⚠ THE 2026-08-09 RECONCILIATION NOTE ABOVE IS THEREFORE NOT A STANDING GUARANTEE. It
        said "ZERO org-only grants were found" and it is retained as the record of what was true
        that day, but it did not hold one day later. Re-run the diff before EVERY deploy of this
        file; do not cite that paragraph as a reason to skip it.

        ⚠ CONSEQUENCE TO KNOW BEFORE DEPLOYING THIS FILE ANYWHERE BUT usman-dpeg: those six
        record types exist in the ORG but NOT in this repo (no recordTypes/ files for them), and
        they are not alone - the org also holds permission sets and a group that were built
        directly in usman-dpeg and never brought into source. Deploying this file to usman-dpeg
        SUCCEEDS because the record types are present there. Deploying it to a scratch org built
        from this repo alone will FAIL on all six. The alternative - leaving them out -
        guarantees a silent wipe on the next deploy to the org that actually matters, which is
        strictly worse. Bringing the unsourced metadata into source is the real fix and is out of
        this change's scope; it is recorded in docs/permission-set-retirement-runbook.md.

        🔴 DO NOT "TIDY" THE SIX recordTypeVisibilities BELOW BECAUSE THEIR RECORD TYPES ARE NOT
        IN THIS REPO. They are live grants in the org. Removing them from this file is exactly
        the silent wipe this whole comment exists to prevent.

        ⚠ RELATED, NOT FIXED HERE, AND MORE DANGEROUS: the DPEG_Principal_PSG group in the ORG
        carries a member that the repo copy of that group does NOT list. A PermissionSetGroup
        deploy REPLACES its member list wholesale, so deploying the repo copy would silently
        revoke that access for every principal. No change in this cleanup deploys that group -
        but anyone who does, for any reason, must reconcile it first.
    -->
    <applicationVisibilities>
        <application>Acquisition</application>
        <visible>true</visible>
    </applicationVisibilities>
    <applicationVisibilities>
        <application>Disposition</application>
        <visible>true</visible>
    </applicationVisibilities>
    <applicationVisibilities>
        <application>Property_Management</application>
        <visible>true</visible>
    </applicationVisibilities>
    <applicationVisibilities>
        <application>Transaction</application>
        <visible>true</visible>
    </applicationVisibilities>
    <!--
        Lead_Convert_Account_RecordType_Stamp added 2026-08-17 (bug fix, LeadConvertServiceTest).
        Measured live against usman-dpeg: the principal Apex tests (and ad hoc admin lead
        conversions) run as holds THIS set but not Lead_Stage_Actions_Access, and Modify All
        Data does not confer a custom-permission grant (see Acquisition_Deal_Driver's identical
        note on this exact point). Without this grant, conversionStampsBrokerRecordTypesOnContactAndAccount
        would still fail post-fix for that principal. See
        Record_Type_Is_Immutable.validationRule-meta.xml and the custom permission's own
        in-file comment for the full mechanism and the accepted scoping tradeoff.
    -->
    <customPermissions>
        <enabled>true</enabled>
        <name>Lead_Convert_Account_RecordType_Stamp</name>
    </customPermissions>
    <description>Restores what the Admin profile grants but never deploys (profiles are .forceignored): every custom tab, eighteen field grants, the four Lightning apps, and the Opportunity/Disposition/NDA/LOI/Account/Contact/Lead record types. See the in-file comment.</description>
    <fieldPermissions>
        <editable>true</editable>
        <field>Contract_Review__c.Disposition__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Disposition__c.NDA_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Disposition__c.Primary_NDA__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Disposition__c.Signed_NDA_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Disposition__c.Wire_Verification_Completed__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>LOI__c.Disposition__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!--
      THE EIGHT ACQUISITION-OBSERVATIONS FIELDS BELOW (four Lead + four Opportunity, added
      2026-08-14) ARE NOT REDUNDANT WITH THE PERSONA SETS. DO NOT REMOVE THEM AS DUPLICATES OF
      DPEG_Acquisition_Edit / _View / DPEG_Opportunity_View / Broker_Protection_Access.

      WHY THEY ARE HERE - MEASURED, NOT ANTICIPATED. Without them the P1-A DEPLOY ITSELF FAILS,
      not merely a persona's visibility. ExtractionScoreUtil.applyExtractionScore assigns the
      three score fields on EVERY path, including the degraded one where all three are null -
      an assignment marks a field DIRTY whether or not the value is null - and a plain Apex
      `insert` on Lead is FLS-ENFORCED in this org. So every Lead insert in the suite now
      touches these fields. Measured by the Apex build in a scratchpad copy: 33 of 36
      EmailToLeadServiceTest methods failed with
          System.DmlException | Operation failed due to fields being inaccessible on Sobject Lead
      INCLUDING tests written months ago that reference none of the new fields. Declaring the
      eight grants cleared all 33. This is the identical symptom ARCHITECTURE.md sec 2 records
      for the 2026-08-05/06 Task.WhoId incident.

      WHY IT IS *THIS* SET. A Metadata-API-deployed custom field arrives with NO field
      permissions for ANY profile, System Administrator included, and profiles/** is
      .forceignore'd - so there is no profile fallback. Test methods run as the DEPLOYING USER
      unless a System.runAs wraps them, and that user is an administrator, not the acquisitions
      persona. The persona sets grant the persona; only this set - which exists precisely to
      restore what the Admin profile grants but never deploys - grants the administrator.
      Granting the persona sets instead does NOT fix the test run.

      EDITABLE, NOT READ-ONLY: the failing operation is a WRITE (an insert that marks the fields
      dirty). readable-only would leave all 33 failing.

      Sequence and verification queries: agent-output/runbook-acquisition-observations-phase1.md
      (step P1-A). These eight must be in the SAME deploy as, or a later one than, the eight
      field definitions - a fieldPermissions entry for a field that does not exist yet fails the
      deploy.
    -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Lead.Extraction_Score_Pct__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lead.Fields_Captured_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lead.Fields_Missing_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lead.Listing_Status__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lease_Inquiry__c.OneDrive_URL__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>NDA__c.Counter_Signed_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>NDA__c.Is_Decline_Allowed__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>NDA__c.Party_Role__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- ADDED 2026-08-14, Acquisition Observations Phase 4 (declarative-only field add).
         Call_For_Offers_Received_Date__c is stamped by CallForOffersStampService. This set is
         load-bearing for the DEPLOY, not just for a screen: RunLocalTests runs as the deploying
         administrator, who holds this set and nothing else, and this org has already been
         measured refusing a plain system-mode insert that merely assigns a brand-new field (see
         the Offer_Alert_* comment below - identical shape, same root cause). Without this grant,
         any test fixture that sets this field fails at insert with "fields being inaccessible on
         Sobject Opportunity", pointing at the new test rather than at this file.
         EDITABLE, matching DPEG_Acquisition_Edit - see that set's grant for Offer_Due_Date__c /
         Sale_Process__c, whose neighbours this field follows. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Call_For_Offers_Received_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Opportunity half of the eight acquisition-observations grants - see the comment above
         the Lead.* block. LeadConvertService carries all four onto the Opportunity at
         conversion, so the same FLS-enforced-write argument applies on this object too. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Extraction_Score_Pct__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Fields_Captured_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Fields_Missing_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Listing_Status__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Phase 4 (call-for-offers alerts, 2026-08-14). The two alert MARKER fields.

         🔴 THIS SET IS THE ONE THAT MATTERS FOR THE DEPLOY ITSELF, NOT JUST FOR AN ADMINISTRATOR'S
         SCREEN. `RunLocalTests` executes as the DEPLOYING ADMINISTRATOR, who is in no permission set
         group and holds this set and nothing else. Metadata-API-deployed custom fields arrive with
         NO field permissions for ANY profile, System Administrator included, and this org has
         already been measured REFUSING a plain system-mode `insert` that merely ASSIGNS a
         brand-new field ("Operation failed due to fields being inaccessible on Sobject Lead" —
         33 of 36 EmailToLeadServiceTest methods, 2026-08-14, including ones written months earlier
         that touched none of the new fields). CallForOffersAlertBatchTest's fixtures set both marker
         fields directly, so without these two entries the deploy that ships the fields fails its own
         test run, and the symptom points at the new code while the cause is a file nobody touched.

         EDITABLE, matching DPEG_Acquisition_Edit: blanking Offer_Alert_Last_Interval__c is the
         supported way to re-arm a deal's alerts. See that set for the full reasoning. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Offer_Alert_Due_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Offer_Alert_Last_Interval__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- FSD Gap Tranche 1, code-review Fix 2 (W2), added 2026-08-16.
         NDA_Alert_Last_Interval__c / NDA_Alert_Expiry_Date__c are the direct NDA__c sibling of
         the Opportunity.Offer_Alert_* markers two entries above, which this set already grants -
         that existing entry is the decisive counter-evidence against the earlier reasoning that
         this set carries only three hand-picked NDA__c fields: alert-marker fields ARE the
         established pattern here, not the exception.
         NOT COSMETIC: NdaExpiryAlertBatch.finish() is deliberately only a debug line BECAUSE the
         durable record of every alert sent lives on these two fields on each NDA - the class
         header calls them "queryable and reportable". Without FLS here, a bare administrator can
         neither query nor report on them, so the one feature whose entire failure mode is
         SILENCE (see gates G1/G2 in agent-output/design-requirements-fsd-tranche-1.md) has its
         only durable record invisible to the person diagnosing it.
         EDITABLE, matching the Offer_Alert_* precedent immediately above and the sibling NDA__c
         fields elsewhere on this set: blanking the marker is the supported way to re-arm a
         deal's alerts, and an administrator correcting one by hand needs write access, not only
         read.
         RECONCILED AGAINST usman-dpeg 2026-08-16 (retrieved via sf project retrieve into a
         scratch directory and diffed field-by-field, record-type-by-record-type, tab-by-tab,
         app-by-app against this file): ZERO differences found on any of the four grant lists -
         fieldPermissions, recordTypeVisibilities, tabSettings, applicationVisibilities all match
         the org exactly. Nothing org-side is at risk of being wiped by this addition; this is a
         pure, additive edit. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>NDA__c.NDA_Alert_Expiry_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>NDA__c.NDA_Alert_Last_Interval__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Property_Package__c provisioning gap, closed 2026-08-17 (code review finding). This set is
         the ONLY one of the seven layers that restores Admin-profile FLS/tabs/apps/record types
         .forceignore'd out of profiles/**, and Property_Package__c FLS was granted in exactly
         three places when it shipped the same day - DPEG_Acquisition_Edit, DPEG_Acquisition_View,
         Broker_Protection_Access - and NOT here. Consequence: a System Administrator holding none
         of those three (this repo's own admin user is one - see the applicationVisibilities
         comment above, "the administrator usman.khan.dpeg is in NO permission set group") gets a
         PERMANENT RED ERROR BANNER on the Lead Funnel homepage instead of the graceful "nothing to
         show" a missing grant produces elsewhere, because BOTH homepage reads are `WITH USER_MODE`
         and a USER_MODE query THROWS on the whole row for one inaccessible field:
           • `PropertyPackageSelector.selectRecent` (the new c/recentPackages widget) selects
             Property_Count__c / Broker_Name__c / Broker_Email__c / Received_DateTime__c directly
             off Property_Package__c.
           • `LeadSelector.selectRecent` (the pre-existing recentLeads Package column, widened
             2026-08-17) selected Lead.Property_Package__c and spanned Property_Package__r.Name off
             it - same query the whole Lead Funnel homepage already depended on. 🔴 CORRECTED
             2026-08-17 (later same day): the recentLeads Package column was removed and
             `LeadSelector.selectRecent` no longer selects `Property_Package__c` at all - this
             bullet's claim is retired, not merely reworded. The `Lead.Property_Package__c` FLS
             grant below (line ~377) is still load-bearing, but now for a DIFFERENT consumer:
             `LeadSelector.selectPackageAnchorById`, which backs the `c/packageSiblings` "Same
             Email" card on the Lead record page. The grant itself is unchanged - only the reason
             it must not be removed has changed.
         `Property_Package__c.Name` needs no grant - custom-object Name fields are
         `permissionable=false` (verified for this object earlier the same day) - and neither does
         the `Property_Package__r.Name` traversal's OBJECT-level read, which `Modify All Data`
         (the System Administrator profile's own system permission) already confers on every
         object; that is also why this set carries no `<objectPermissions>` block for
         Property_Package__c, matching every other object restored here.
         SCOPED NARROWLY TO THE REPORTED GAP: the Opportunity.Property_Package__c and
         Property__c.Property_Package__c lookups are NOT granted here - neither
         `PropertyPackageSelector.selectRecent` nor `LeadSelector.selectRecent` reads them; they
         back the separate c/packageSiblings card on the Opportunity/Property record pages, which
         is not the homepage this finding is about. Add them here only if a future admin-visibility
         gap is measured against THAT feature. -->
    <fieldPermissions>
        <editable>false</editable>
        <field>Property_Package__c.Broker_Email__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Property_Package__c.Broker_Name__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Property_Package__c.Property_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Property_Package__c.Received_DateTime__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Lead.Property_Package__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <hasActivationRequired>false</hasActivationRequired>
    <label>DPEG Admin Access</label>
    <!--
      DE-DUPLICATED 2026-08-14. The block that stood here declared SIX recordTypeVisibilities
      a second time - Account.Broker_Firm, Account.Investor_Entity, Contact.Broker,
      Contact.Investor, Lead.Acquisition_Broker, Lead.IR_Investor - all of which were already
      present in the sorted list below (and still are). It has been removed; each entry now
      appears exactly once, which is what the org holds.

      HOW IT GOT HERE, because the mistake is repeatable. The removed block carried a comment
      stating the six were "live on this set in usman-dpeg and missing from this file, so the
      next deploy of it would have destroyed them". The first half was true; the second was
      not. They were not missing - they were in the alphabetically sorted list further down,
      which is easy to miss because the appended copies sorted ABOVE it and so read as a
      separate, additional group. The 2026-08-10 reconciliation compared the ORG's entries
      against a scan of this file and appended what it believed was absent.

      THE GENERAL LESSON: when reconciling org -> repo, diff the SETS, not the presence of a
      block. A grep for a record type in a 400-line file that is sorted in one region and
      appended in another will answer "not found" for something that is present.

      MEASURED, not inferred: a retrieve of this set from usman-dpeg on 2026-08-14 returns
      16 recordTypeVisibilities, each exactly once; this file held 22, the six extras being
      precisely those duplicates.

      PRESERVED from the removed comment because it is a genuine, separate observation and is
      still true: the Account.* and Contact.* record types referenced below exist in the ORG
      but have no recordType metadata in this repo. That is why they cannot be verified by a
      file-based check, and it is unrelated to the duplication.

      Opportunity.Commercial WAS RETIRED AT PHASE 1 D4 and has been removed from this set;
      Opportunity.Retail replaced it. The two coexisted from D1 through the migration window on
      purpose. Do not re-add Commercial - the record type no longer exists and the deploy would
      fail. Sequence: agent-output/runbook-acquisition-observations-phase1.md.
    -->
    <recordTypeVisibilities>
        <recordType>Account.Broker_Firm</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Account.Investor_Entity</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Contact.Broker</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Contact.Investor</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Contract_Review__c.Acquisition_PSA</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Contract_Review__c.Disposition_PSA</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Disposition__c.Off_Market</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Disposition__c.On_Market</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>LOI__c.Acquisition_LOI</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>LOI__c.Disposition_LOI</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Lead.Acquisition_Broker</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Lead.IR_Investor</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>NDA__c.Acquisition_NDA</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>NDA__c.Disposition_NDA</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Opportunity.Land</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Opportunity.Retail</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <tabSettings>
        <tab>Acquisition_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Active_Transactions</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Broker_Assignment__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Broker_Assignments</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Broker_Hub</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Broker_Scorecard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Construction_Feasibility_Review__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Contract_Review__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Development_Feasibility_Review__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Disposition_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Disposition__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>LOI__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lead_Funnel</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease_Activity_Tracker</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease_Inquiry__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease_Renewal__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease_Renewals</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Leasing_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>NDA__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Offering__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Onboarding</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Onboarding__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Property_Asset__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Property_Management_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Property__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Sell_Meter</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Transaction_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Transaction__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Underwriting__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Work_Order_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Work_Order__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Work_Orders</tab>
        <visibility>Visible</visibility>
    </tabSettings>
</PermissionSet>
