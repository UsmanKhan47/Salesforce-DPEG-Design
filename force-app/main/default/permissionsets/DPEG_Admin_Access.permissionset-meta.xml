<?xml version="1.0" encoding="UTF-8"?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <!--
        This comment must sit INSIDE the root element. A comment placed ABOVE <PermissionSet>
        breaks `sf` deploy at conversion with the misleading error "unable to find matching
        parent xml file". Broker_Protection_Access is the precedent for this pattern; it is
        used here because the rationale below is far longer than the 255-character cap on
        <description>.

        WHY Disposition__c.Wire_Verification_Completed__c IS GRANTED HERE (added 2026-08-09,
        code review W2). Metadata-API-deployed custom fields arrive with NO field permissions
        for ANY profile, System Administrator included - a trap this repo has already paid for
        (see ARCHITECTURE.md and the deal-driver gate incident, where a bare admin threw on the
        very field they had just deployed). The consequence here is worse than an admin simply
        not seeing a field: AN APPROVAL PAGE SILENTLY OMITS FIELDS THE APPROVER CANNOT READ.
        Wire_Verification_Completed__c is on Closing_Approval's approvalPageFields precisely so
        the approver sees the wire state when deciding (Gate 1, Q4), so without this grant an
        administrator acting as approver gets a Closing approval page with the wire state
        simply absent - no error, no blank, nothing. The design fails invisibly, which is the
        outcome Q4 existed to prevent.

        READ-ONLY on purpose: the field is DERIVED, maintained by the Wire_Verification_Rollup
        flow from the most recent Wire, and its own <description> says DO NOT HAND-EDIT. An
        editable grant would let an admin hand-set the anti-fraud gate the validation rule
        reads. Contrast Lease_Inquiry__c.OneDrive_URL__c below, which is editable because it is
        hand-entered.

        The two NAMED approvers do not depend on this set - they are covered by
        DPEG_Principal_PSG -> DPEG_Disposition_View, which already carries readable=true on the
        same field. This entry closes the ADMIN approver case only.

        POST-DEPLOY GATE (design section 8, gate A4): after deploying, open the Closing approval
        page as EACH persona that can approve - including an administrator - and confirm the
        Wire Verification Completed row is actually rendered. An admin smoke test alone proves
        nothing about the named approvers, and vice versa; FLS truth lives in the org, not in
        this repo, because profiles/** is .forceignore'd.

        WARNING: a PermissionSet deploy REPLACES this file's entire <fieldPermissions> set. Any
        org-side-only FLS grant not represented here is silently wiped by the next deploy of
        this file, even one made for an unrelated reason. Reconcile against the org before
        deploying; do not "keep it org-side so a redeploy can't disturb it".

        RECONCILED AGAINST THE ORG 2026-08-09 (Tranche 3B). This set, together with
        DPEG_Acquisition_Edit, DPEG_Acquisition_View and DPEG_Acquisitions, was retrieved from
        the default org and compared field-by-field, object-by-object, record-type-by-record-type
        against the repo copy: ZERO org-only grants were found in any of the four, so nothing
        in the org is at risk of being wiped by this deploy. Every difference ran the other way
        (repo ahead of org) and is the Tranche 2 / 3A / 3B work that has not been deployed yet.

        WHY LOI__c.Disposition__c IS GRANTED HERE (added 2026-08-09, Tranche 3B). Same reason as
        the Disposition__c fields above: Metadata-API-deployed custom fields arrive with NO field
        permissions for ANY profile, System Administrator included, so without this entry an
        administrator cannot see or set the lookup that distinguishes a sell-side LOI from a
        buy-side one. It is EDITABLE, unlike the derived counters above, because it is
        hand-entered (or pre-filled by a related-list New button) rather than maintained by a
        flow. The two new LOI record types are granted below for the same class of reason - a
        record type is invisible until it is granted, and Modify All Data is an OBJECT
        permission that confers no record-type access.

        ══════════════════════════════════════════════════════════════════════════════════
        2026-08-10 - PERMISSION SET CLEANUP. TWO CHANGES, AND THE SECOND IS A BUG FIX.
        ══════════════════════════════════════════════════════════════════════════════════

        (A) THE FOUR applicationVisibilities WERE MOVED HERE FROM Acquisition_App_Access
        (Gate 1, Q3). That set is being retired: its four app grants are 100% covered by
        DPEG_App_Acquisition / _Disposition / _Transaction / _PropertyMgmt for every PERSONA,
        but the administrator usman.khan.dpeg is in NO permission set group and holds NONE of
        those four sets, so Acquisition_App_Access was the only thing making the four Lightning
        apps visible to them. Because profiles/** is .forceignore'd, deleting it without this
        move would remove the admin's own App Launcher entries with nothing in source to restore
        them. Putting the grants HERE is what makes the restoration DEPLOYABLE - which is the
        stated purpose of this set - rather than four manual assignments repeated on every org
        rebuild. THE APPS MUST BE VISIBLE HERE AND VERIFIED BEFORE Acquisition_App_Access IS
        UNASSIGNED. Never the other way round.

        (B) 🔴 SIX recordTypeVisibilities WERE ADDED TO STOP THIS FILE SILENTLY WIPING THEM.
        Account.Broker_Firm, Account.Investor_Entity, Contact.Broker, Contact.Investor,
        Lead.Acquisition_Broker and Lead.IR_Investor were LIVE IN THE ORG on this set and were
        ABSENT FROM THIS FILE. A PermissionSet deploy REPLACES the whole grant list rather than
        merging into it, so the very next deploy of this file - including the app move in (A),
        made for an entirely unrelated reason - would have destroyed all six with no error and no
        warning. That is the identical failure that took down Broker Protection twice
        (2026-08-05 and 2026-08-06). Measured by retrieving this set from usman-dpeg into a
        scratch directory and diffing normalized grant lists: org 59 grants, repo 53, six
        org-only, zero repo-only.

        ⚠ THE 2026-08-09 RECONCILIATION NOTE ABOVE IS THEREFORE NOT A STANDING GUARANTEE. It
        said "ZERO org-only grants were found" and it is retained as the record of what was true
        that day, but it did not hold one day later. Re-run the diff before EVERY deploy of this
        file; do not cite that paragraph as a reason to skip it.

        ⚠ CONSEQUENCE TO KNOW BEFORE DEPLOYING THIS FILE ANYWHERE BUT usman-dpeg: those six
        record types exist in the ORG but NOT in this repo (no recordTypes/ files for them), and
        they are not alone - the org also holds permission sets and a group that were built
        directly in usman-dpeg and never brought into source. Deploying this file to usman-dpeg
        SUCCEEDS because the record types are present there. Deploying it to a scratch org built
        from this repo alone will FAIL on all six. The alternative - leaving them out -
        guarantees a silent wipe on the next deploy to the org that actually matters, which is
        strictly worse. Bringing the unsourced metadata into source is the real fix and is out of
        this change's scope; it is recorded in docs/permission-set-retirement-runbook.md.

        🔴 DO NOT "TIDY" THE SIX recordTypeVisibilities BELOW BECAUSE THEIR RECORD TYPES ARE NOT
        IN THIS REPO. They are live grants in the org. Removing them from this file is exactly
        the silent wipe this whole comment exists to prevent.

        ⚠ RELATED, NOT FIXED HERE, AND MORE DANGEROUS: the DPEG_Principal_PSG group in the ORG
        carries a member that the repo copy of that group does NOT list. A PermissionSetGroup
        deploy REPLACES its member list wholesale, so deploying the repo copy would silently
        revoke that access for every principal. No change in this cleanup deploys that group -
        but anyone who does, for any reason, must reconcile it first.

        ══════════════════════════════════════════════════════════════════════════════════
        P8 HUB PASS (2026-08-30) - §3 DECISION, RECORDED HERE ON PURPOSE. Lead.Parse_Confidence__c,
        Lead.Deal_Notes__c and Lead.First_Seen_Date__c are measured at ZERO FieldPermissions rows
        on this set (same as on Broker_Protection_Access). DELIBERATELY NOT BACKFILLED HERE,
        unlike on Broker_Protection_Access where all but Parse_Confidence__c were backfilled.
        WHY THE TWO SETS WERE TREATED DIFFERENTLY: every existing grant in THIS file traces to a
        MEASURED failure - a deploy/test run that broke (the eight-fields block above, the
        Call_For_Offers/Offer_Alert/NDA_Alert blocks) or an approval page that silently omitted a
        field (Wire_Verification_Completed__c). None of these three fields has a measured failure
        of that kind: the gap is a HUMAN VISIBILITY gap only (the columns render blank for an
        administrator), which is real but is not the class of problem this set exists to solve -
        widening its purpose to "administrator should see every Lead field" would make it a
        second, competing copy of DPEG_Acquisition_View rather than the narrow deploy-unblocking
        set it has been kept as through every prior addition. The hub-pass's own §3 recommendation
        text names Broker_Protection_Access only ("backfill those three on Broker_Protection_Access
        in the same pass") - it does not name this set, which agrees with the reasoning above
        rather than being silent on it. If an administrator smoke-testing a Lead record ever
        needs these three fields and cannot see them, that is the trigger to revisit this decision
        specifically, not to re-apply it here speculatively now.

        🔴 CORRECTED SAME DAY (BA Gap-Closure code-review pass, C2) - Lead.Parse_Confidence__c IS
        NOW GRANTED BELOW, READ-ONLY, AND IS NO LONGER COVERED BY THE "no measured failure" REASONING
        ABOVE. The premise changed hours after this decision was written: WS7 promoted the field
        from a display column into a `Review_Queue` list-view FILTER term
        (`Parse_Confidence__c = LOW OR Low_Confidence_Field_Count__c > 0`). This set's
        applicationVisibilities restore the Acquisition app for this administrator, so the same
        Review Queue list view is reachable here too - an unreadable filter field is now the same
        class of MEASURED-adjacent gap this set exists to close (see the eight-fields comment
        above), not a human-visibility nicety. Deal_Notes__c and First_Seen_Date__c are UNCHANGED
        by this correction - they are plain display columns, not filter terms, so the original
        reasoning still applies to them and they remain deliberately absent from this set.
        ══════════════════════════════════════════════════════════════════════════════════
    -->
    <applicationVisibilities>
        <application>Acquisition</application>
        <visible>true</visible>
    </applicationVisibilities>
    <applicationVisibilities>
        <application>Disposition</application>
        <visible>true</visible>
    </applicationVisibilities>
    <applicationVisibilities>
        <application>Property_Management</application>
        <visible>true</visible>
    </applicationVisibilities>
    <applicationVisibilities>
        <application>Transaction</application>
        <visible>true</visible>
    </applicationVisibilities>
    <!--
        Disposition_Record_Type_Override ADDED 2026-08-31, Disposition Gap Closure Tranche 1
        code review finding C-2. The custom permission had been created
        (customPermissions/Disposition_Record_Type_Override) and was cited by BOTH the validation
        rule and its own header as owed to THIS set "in the consolidated permission-set pass" -
        but that pass landed nine field grants and missed this one. Measured repo wide before this
        edit: no permission set anywhere referenced the permission. It existed and was granted to
        nobody.

        WHAT THIS GRANT IS. It is the ONLY escape hatch for
        objects/Disposition__c/validationRules/Record_Type_Is_Immutable, which locks
        Disposition__c.RecordTypeId once a disposition has committed past its opening stage.

        THE LOCK BOUNDARY - USER DECISION AT GATE 1, 2026-08-31. The disposition record type is
        changeable ONLY while Disposition_Stage__c is 'Disposition Readiness'; it is LOCKED from
        'BOV Outreach' onward through 'Sale Closes'. Rationale in the user's own terms: a principal
        approves a specific disposition path at Sale_Decision_Approval, whose entry criterion is
        Disposition_Stage__c = 'Disposition Readiness', so nothing downstream of THAT approval may
        silently change it. "Past Readiness" and "past the sale decision" are the same boundary.

        🔴 MODIFY ALL DATA DOES NOT CONFER THIS. A validation rule is not bypassed by Modify All
        Data, and a custom permission is not conferred by the System Administrator profile or by
        Modify All Data either - the identical point Lead_Convert_Account_RecordType_Stamp's block
        immediately below records for its own gate. Holding a permission set that enables the
        permission is the ONLY route to the override; there is no other bypass. Without this entry
        Record_Type_Is_Immutable deploys green as an ABSOLUTE lock with no escape for anyone,
        $Permission.Disposition_Record_Type_Override evaluates false for every user, and story 2's
        "admin overridable" acceptance criterion silently does not exist. 🔴 NO DRY RUN DETECTS
        THIS: nothing about the rule, the permission or this set is malformed - the defect is
        purely an absent grant.

        ⚠ $Permission gates by WHO the running user is, not by WHICH CODE PATH is running. Any
        holder of this set could in principle change an existing disposition's RecordTypeId by hand
        (UI edit, Data Loader) while the permission is enabled. That is the accepted tradeoff
        recorded in the custom permission's own header, which also states the grant is to
        DPEG_Admin_Access ONLY. Do not enable it on any other set without first re reading that
        note.

        🔴 DEPLOY ORDER: customPermissions/Disposition_Record_Type_Override must land in the SAME
        deploy as this file or an EARLIER one. A customPermissions entry naming a permission the
        target org does not yet hold fails the deploy of this entire set - unlike the validation
        rule, which tolerates the reverse order by simply evaluating the merge field as false.

        RECONCILED AGAINST usman-dpeg 2026-08-31, IMMEDIATELY BEFORE THIS EDIT. Retrieved with
        sf project retrieve start into a scratch directory OUTSIDE the project, then diffed by a
        parser over normalized, comment stripped grant lists: org 96 grants, repo 96 grants, ZERO
        org only and ZERO repo only across applicationVisibilities (4), customPermissions (1),
        fieldPermissions (42), recordTypeVisibilities (16) and tabSettings (33). Nothing org side
        is at risk of being wiped by this addition; it is a purely additive edit. The retrieve was
        confirmed non empty and both sides parsed to 96, so the comparison is not vacuous. Note the
        org copy's lastModifiedDate was 2026-08-31T06:55:38Z - it HAD been touched the same day,
        which is exactly why the diff was measured rather than assumed. As the 2026-08-10 block at
        the top of this file warns, neither that paragraph nor this one is a standing guarantee;
        re run the diff before EVERY deploy of this file.

        ⚠ ASSIGNMENT REALITY - MEASURED 2026-08-31, REPORTED NOT FIXED. This set has exactly ONE
        assignee: usman.khan.dpeg@avanzasolutions.com (active, assigned DIRECTLY, PermissionSetGroupId
        null). PermissionSetGroupComponent returns ZERO rows for it, and no permissionsetgroups/**
        file in this repo names it, so no permission set group includes it. The override therefore
        reaches that single administrator and nobody else. If any other administrator must be able
        to lift the lock, this set has to be assigned to them - a PermissionSetAssignment is not
        deployable metadata and is deliberately out of scope for this fix.
    -->
    <customPermissions>
        <enabled>true</enabled>
        <name>Disposition_Record_Type_Override</name>
    </customPermissions>
    <!--
        Lead_Convert_Account_RecordType_Stamp added 2026-08-17 (bug fix, LeadConvertServiceTest).
        Measured live against usman-dpeg: the principal Apex tests (and ad hoc admin lead
        conversions) run as holds THIS set but not Lead_Stage_Actions_Access, and Modify All
        Data does not confer a custom-permission grant (see Acquisition_Deal_Driver's identical
        note on this exact point). Without this grant, conversionStampsBrokerRecordTypesOnContactAndAccount
        would still fail post-fix for that principal. See
        Record_Type_Is_Immutable.validationRule-meta.xml and the custom permission's own
        in-file comment for the full mechanism and the accepted scoping tradeoff.
    -->
    <customPermissions>
        <enabled>true</enabled>
        <name>Lead_Convert_Account_RecordType_Stamp</name>
    </customPermissions>
    <description>Restores what the Admin profile grants but never deploys (profiles are .forceignored): every custom tab, eighteen field grants, the four Lightning apps, and the Opportunity/Disposition/NDA/LOI/Account/Contact/Lead record types. See the in-file comment.</description>
    <fieldPermissions>
        <editable>true</editable>
        <field>Contract_Review__c.Disposition__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Disposition__c.NDA_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Disposition__c.Primary_NDA__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Disposition__c.Signed_NDA_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Disposition__c.Wire_Verification_Completed__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>LOI__c.Disposition__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!--
      THE EIGHT ACQUISITION-OBSERVATIONS FIELDS BELOW (four Lead + four Opportunity, added
      2026-08-14) ARE NOT REDUNDANT WITH THE PERSONA SETS. DO NOT REMOVE THEM AS DUPLICATES OF
      DPEG_Acquisition_Edit / _View / DPEG_Opportunity_View / Broker_Protection_Access.

      WHY THEY ARE HERE - MEASURED, NOT ANTICIPATED. Without them the P1-A DEPLOY ITSELF FAILS,
      not merely a persona's visibility. ExtractionScoreUtil.applyExtractionScore assigns the
      three score fields on EVERY path, including the degraded one where all three are null -
      an assignment marks a field DIRTY whether or not the value is null - and a plain Apex
      `insert` on Lead is FLS-ENFORCED in this org. So every Lead insert in the suite now
      touches these fields. Measured by the Apex build in a scratchpad copy: 33 of 36
      EmailToLeadServiceTest methods failed with
          System.DmlException | Operation failed due to fields being inaccessible on Sobject Lead
      INCLUDING tests written months ago that reference none of the new fields. Declaring the
      eight grants cleared all 33. This is the identical symptom ARCHITECTURE.md sec 2 records
      for the 2026-08-05/06 Task.WhoId incident.

      WHY IT IS *THIS* SET. A Metadata-API-deployed custom field arrives with NO field
      permissions for ANY profile, System Administrator included, and profiles/** is
      .forceignore'd - so there is no profile fallback. Test methods run as the DEPLOYING USER
      unless a System.runAs wraps them, and that user is an administrator, not the acquisitions
      persona. The persona sets grant the persona; only this set - which exists precisely to
      restore what the Admin profile grants but never deploys - grants the administrator.
      Granting the persona sets instead does NOT fix the test run.

      EDITABLE, NOT READ-ONLY: the failing operation is a WRITE (an insert that marks the fields
      dirty). readable-only would leave all 33 failing.

      Sequence and verification queries: agent-output/runbook-acquisition-observations-phase1.md
      (step P1-A). These eight must be in the SAME deploy as, or a later one than, the eight
      field definitions - a fieldPermissions entry for a field that does not exist yet fails the
      deploy.
    -->
    <!-- Deal_Name__c ADDED P8 HUB PASS (2026-08-30), WS6. Formula, now a column on all five Lead
         list views, including the ones this administrator's App Launcher exposes via the
         applicationVisibilities restored on this set. Read-only: formula fields cannot carry an
         editable grant. -->
    <fieldPermissions>
        <editable>false</editable>
        <field>Lead.Deal_Name__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lead.Extraction_Score_Pct__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lead.Fields_Captured_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Low_Confidence_Field_Count__c ADDED P8 HUB PASS (2026-08-30), WS7. Per the hub-pass
         matrix this set needs read only, not edit - it is the Review Queue list view's filter
         field, and this set's job is administrator VISIBILITY (see the eight-fields comment
         above this block for why a WRITE-breaking grant lives here at all: it is only added when
         a measured deploy/test/approval-page failure requires it, and none was measured for this
         field). Unlike Field_Confidence_JSON__c/Low_Confidence_Field_Count__c's mandatory EDIT
         grant on DPEG_Acquisition_Edit, EmailToLeadService.applyFieldConfidence's isCreateable()
         guard means a missing Edit grant here does NOT fail the administrator's own Lead insert
         (RunLocalTests) - it only silently skips populating the field for that run, which read
         access does not fix either way. Read-only closes the filter/visibility gap without
         widening this set beyond its established narrow purpose. -->
    <fieldPermissions>
        <editable>false</editable>
        <field>Lead.Low_Confidence_Field_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Parse_Confidence__c ADDED 2026-08-30, BA Gap-Closure code-review pass, C2. Read-only,
         same reasoning as Low_Confidence_Field_Count__c immediately above: both are the
         Review Queue list view's filter fields (`Parse_Confidence__c = LOW OR
         Low_Confidence_Field_Count__c > 0`), and this set's own §3 decision block (top of file)
         records why this field was withheld until now and why it changed. -->
    <fieldPermissions>
        <editable>false</editable>
        <field>Lead.Parse_Confidence__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lead.Fields_Missing_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lead.Listing_Status__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Lease_Inquiry__c.OneDrive_URL__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>NDA__c.Counter_Signed_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>NDA__c.Is_Decline_Allowed__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>NDA__c.Party_Role__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- ADDED 2026-08-14, Acquisition Observations Phase 4 (declarative-only field add).
         Call_For_Offers_Received_Date__c is stamped by CallForOffersStampService. This set is
         load-bearing for the DEPLOY, not just for a screen: RunLocalTests runs as the deploying
         administrator, who holds this set and nothing else, and this org has already been
         measured refusing a plain system-mode insert that merely assigns a brand-new field (see
         the Offer_Alert_* comment below - identical shape, same root cause). Without this grant,
         any test fixture that sets this field fails at insert with "fields being inaccessible on
         Sobject Opportunity", pointing at the new test rather than at this file.
         EDITABLE, matching DPEG_Acquisition_Edit - see that set's grant for Offer_Due_Date__c /
         Sale_Process__c, whose neighbours this field follows. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Call_For_Offers_Received_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Opportunity half of the eight acquisition-observations grants - see the comment above
         the Lead.* block. LeadConvertService carries all four onto the Opportunity at
         conversion, so the same FLS-enforced-write argument applies on this object too. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Extraction_Score_Pct__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Fields_Captured_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Fields_Missing_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Listing_Status__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Phase 4 (call-for-offers alerts, 2026-08-14). The two alert MARKER fields.

         🔴 THIS SET IS THE ONE THAT MATTERS FOR THE DEPLOY ITSELF, NOT JUST FOR AN ADMINISTRATOR'S
         SCREEN. `RunLocalTests` executes as the DEPLOYING ADMINISTRATOR, who is in no permission set
         group and holds this set and nothing else. Metadata-API-deployed custom fields arrive with
         NO field permissions for ANY profile, System Administrator included, and this org has
         already been measured REFUSING a plain system-mode `insert` that merely ASSIGNS a
         brand-new field ("Operation failed due to fields being inaccessible on Sobject Lead" —
         33 of 36 EmailToLeadServiceTest methods, 2026-08-14, including ones written months earlier
         that touched none of the new fields). CallForOffersAlertBatchTest's fixtures set both marker
         fields directly, so without these two entries the deploy that ships the fields fails its own
         test run, and the symptom points at the new code while the cause is a file nobody touched.

         EDITABLE, matching DPEG_Acquisition_Edit: blanking Offer_Alert_Last_Interval__c is the
         supported way to re-arm a deal's alerts. See that set for the full reasoning. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Offer_Alert_Due_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Offer_Alert_Last_Interval__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Offer_Status__c ADDED Acquisitions gap-fix Item 5a/FIX 3 (2026-08-30). Same class of
         gap, and same load-bearing-for-the-DEPLOY reasoning, as the
         Call_For_Offers_Received_Date__c grant above: OpportunitySelector's Call for Offers
         alert queries (queryCallForOffersAlerts, and the suppression predicate added in this
         same tranche) run WITH USER_MODE, and this org has already been measured refusing the
         WHOLE ROW - not degrading gracefully - on a single inaccessible field under USER_MODE.
         RunLocalTests executes as the deploying administrator, who holds this set and nothing
         else, so without this grant both call-for-offers surfaces (the alert query and any test
         fixture that sets Offer_Status__c) break for the person shipping the change, not just
         for an end user. EDITABLE, matching Call_For_Offers_Received_Date__c's precedent
         immediately above and DPEG_Acquisition_Edit's grant on this same field. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Offer_Status__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Stage_Alert_Last_Sent_Date__c / Stage_Alert_Stage__c ADDED Acquisitions gap-fix Item 4
         (2026-08-31). Same class of gap as Offer_Alert_Due_Date__c/Offer_Alert_Last_Interval__c
         and NDA_Alert_Expiry_Date__c/NDA_Alert_Last_Interval__c below: Metadata-API-deployed
         custom fields arrive with NO field permissions for ANY profile, System Administrator
         included, and this org has already been measured REFUSING a plain system-mode insert
         that merely assigns a brand-new field. RunLocalTests executes as the deploying
         administrator, who holds this set and nothing else, so without these two grants any test
         fixture that sets either marker directly (StageDelayAlertBatchTest, following the
         established CallForOffersAlertBatchTest / NdaExpiryAlertBatchTest pattern of pre-setting
         markers to test idempotency) breaks the deploy for the person shipping the change.
         EDITABLE, matching the Offer_Alert_*/NDA_Alert_* precedent in this set - NOT the
         read-only grant these two fields carry on DPEG_Acquisition_Edit/View and
         DPEG_Opportunity_View, which is a deliberate, separately-reasoned divergence for
         end-user-facing sets (see DPEG_Acquisition_Edit's own comment on these fields). -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Stage_Alert_Last_Sent_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Opportunity.Stage_Alert_Stage__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Deals_Lost__c / First_Submission_DateTime__c ADDED Acquisitions gap-fix Item 6.2/6.4
         (2026-08-31). Same RunLocalTests-deploying-administrator reasoning as the Opportunity
         marker fields immediately above and the Account roll-ups below: BrokerCounterRecalcBatch
         is the maintainer, and any test fixture that pre-sets a starting count directly (the
         reset-to-zero test named in design-requirements.md §2 ITEM 6.2/6.4 needs a Contact with
         an existing nonzero count as its starting fixture) needs this grant to deploy cleanly.
         EDITABLE here, UNLIKE the read-only grant on DPEG_Contact_Edit/View - see that set's
         comment for why the divergence is deliberate. See Deals_Lost__c.field-meta.xml for the
         population caveat both fields carry. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>Contact.Deals_Lost__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>Contact.First_Submission_DateTime__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- FSD Gap Tranche 1, code-review Fix 2 (W2), added 2026-08-16.
         NDA_Alert_Last_Interval__c / NDA_Alert_Expiry_Date__c are the direct NDA__c sibling of
         the Opportunity.Offer_Alert_* markers two entries above, which this set already grants -
         that existing entry is the decisive counter-evidence against the earlier reasoning that
         this set carries only three hand-picked NDA__c fields: alert-marker fields ARE the
         established pattern here, not the exception.
         NOT COSMETIC: NdaExpiryAlertBatch.finish() is deliberately only a debug line BECAUSE the
         durable record of every alert sent lives on these two fields on each NDA - the class
         header calls them "queryable and reportable". Without FLS here, a bare administrator can
         neither query nor report on them, so the one feature whose entire failure mode is
         SILENCE (see gates G1/G2 in agent-output/design-requirements-fsd-tranche-1.md) has its
         only durable record invisible to the person diagnosing it.
         EDITABLE, matching the Offer_Alert_* precedent immediately above and the sibling NDA__c
         fields elsewhere on this set: blanking the marker is the supported way to re-arm a
         deal's alerts, and an administrator correcting one by hand needs write access, not only
         read.
         RECONCILED AGAINST usman-dpeg 2026-08-16 (retrieved via sf project retrieve into a
         scratch directory and diffed field-by-field, record-type-by-record-type, tab-by-tab,
         app-by-app against this file): ZERO differences found on any of the four grant lists -
         fieldPermissions, recordTypeVisibilities, tabSettings, applicationVisibilities all match
         the org exactly. Nothing org-side is at risk of being wiped by this addition; this is a
         pure, additive edit. -->
    <fieldPermissions>
        <editable>true</editable>
        <field>NDA__c.NDA_Alert_Expiry_Date__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>NDA__c.NDA_Alert_Last_Interval__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Portfolio_Deal__c (Property Package to Portfolio Deal rename; grants added Phase B1/B2,
         2026-08-18; the Property_Package__c twin block REMOVED here in Phase B5, same day).

         WHY THIS SET CARRIES THESE GRANTS AT ALL (original 2026-08-17 code review finding, still
         the live reason, only the object name changed): this set is the ONLY one of the seven
         layers that restores Admin profile FLS/tabs/apps/record types .forceignore'd out of
         profiles/**. When Property_Package__c shipped, its FLS was granted in exactly three places
         (DPEG_Acquisition_Edit, DPEG_Acquisition_View, Broker_Protection_Access) and NOT here.
         Consequence: a System Administrator holding none of those three (this repo's own admin user
         is one; see the applicationVisibilities comment above, "the administrator usman.khan.dpeg
         is in NO permission set group") got a PERMANENT RED ERROR BANNER on the Lead Funnel
         homepage instead of the graceful "nothing to show" a missing grant produces elsewhere,
         because the homepage read is `WITH USER_MODE` and a USER_MODE query THROWS on the whole row
         for one inaccessible field. That is still true of the Portfolio_Deal__c grants below.

         LOAD-BEARING CONSUMERS (both live as of Phase B4, deployed 2026-08-18):
           • `PortfolioDealSelector.selectRecent` (renamed from `PropertyPackageSelector`) selects
             Property_Count__c / Broker_Name__c / Broker_Email__c / Received_DateTime__c directly
             off Portfolio_Deal__c, for `c/recentPortfolioDeals` (renamed from `c/recentPackages`)
             on the Lead Funnel homepage. The four field grants below are the only thing standing
             between a group-less admin and that red banner.
           • `LeadSelector.selectPackageAnchorById` reads the Lead lookup, backing the record page
             card `c/portfolioDealSiblings` ("Portfolio Deal", renamed from `c/packageSiblings` /
             "Same Email"). This is a CORRECTION carried forward from the block this replaces:
             `LeadSelector.selectRecent` is NOT a consumer. The recentLeads Package column was
             removed on 2026-08-17 and that selector no longer reads either lookup. The claim that
             it did is retired, not merely reworded, and is repeated here so it is not rediscovered
             the hard way.

         SCOPE IS UNCHANGED BY THE RENAME, AND IS DELIBERATELY NARROWER THAN THE OTHER SETS: four
         fields, not six, and the Lead lookup only. Four fields plus one lookup out, four fields
         plus one lookup in. One grant for one grant. The original scoping decision was that the
         Opportunity.Property_Package__c and Property__c.Property_Package__c lookups are NOT granted
         here because they back the siblings card on the Opportunity/Property record pages, not the
         homepage the finding was about, and that they be added "only if a future admin visibility
         gap is measured against THAT feature". No such gap has been measured, so the equivalent
         Portfolio_Deal__c lookups are likewise absent. Widening the scope now would silently
         overturn a documented code review decision under cover of a rename. The two fields absent
         on both twins are Routed_Outcomes__c and Source_Staging_Id__c.

         Portfolio_Deal__c.Name needs no grant: custom object Name fields are permissionable=false.
         No `<objectPermissions>` block for Portfolio_Deal__c, matching the twin it replaces and
         every other object restored on this set: `Modify All Data` on the System Administrator
         profile already confers object level access, which also covers the `Portfolio_Deal__r.Name`
         traversal's OBJECT level read.

         WHAT PHASE B5 REMOVED FROM THIS SET: four Property_Package__c.<field> grants and one
         Lead.Property_Package__c lookup grant. (No Property_Package__c `<objectPermissions>` block
         existed here to remove, unlike on DPEG_Acquisition_Edit / DPEG_Acquisition_View /
         Broker_Protection_Access, for the `Modify All Data` reason above.) The org drops those
         entries by itself when the object is deleted, but this FILE would not, and a permission set
         whose XML names a field that no longer exists fails the deploy of the whole set the next
         time anyone touches it. So the removal ships in the same wave as the deletion, in the
         constructive half, which lands BEFORE the post-destructive half. -->

    <fieldPermissions>
        <editable>false</editable>
        <field>Portfolio_Deal__c.Broker_Email__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Portfolio_Deal__c.Broker_Name__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Portfolio_Deal__c.Property_Count__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Portfolio_Deal__c.Received_DateTime__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Lead.Portfolio_Deal__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Lead.Property_Address__c / Opportunity.Property_Address__c ADDED 2026-08-19. Same class of
         gap as the Portfolio_Deal__c block above: PortfolioDealSelector.selectWithMembersById (via
         PortfolioDealController.getSiblingRecords) is about to add Property_Address__c to its
         SELECT list under WITH USER_MODE, and a missing FLS grant on a USER_MODE query THROWS on
         the whole row, not just the one field - it does not degrade gracefully, it blanks the
         portfolioDealSiblings card with a red error banner. Read-only: this is a display-only card
         with no write path through this selector. -->
    <fieldPermissions>
        <editable>false</editable>
        <field>Lead.Property_Address__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Opportunity.Property_Address__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Total_Deals_Submitted__c / Deals_Won__c / Deals_Lost__c ADDED Acquisitions gap-fix
         Item 6.1/FIX 4 (2026-08-30). Roll-up summaries on Account (broker firm), read-only by
         type - a roll-up summary can never carry editable=true, that fails the deploy outright.
         Granted here, not only on DPEG_Account_View, because BrokerFirmController now reads
         these three fields to render the Broker Firm sidebar card on the Opportunity record
         page - a live UI read path, not only a report - and this set is the one RunLocalTests
         runs under (see the Call_For_Offers_Received_Date__c / Offer_Status__c comments above
         for the fuller USER_MODE/RunLocalTests reasoning; BrokerFirmController's own selector
         read is the same shape). Same three fields, same reasoning, also added this pass to
         DPEG_Acquisition_Edit, DPEG_Acquisition_View and DPEG_Opportunity_View - every set whose
         persona opens an Opportunity record page. -->
    <fieldPermissions>
        <editable>false</editable>
        <field>Account.Deals_Lost__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Account.Deals_Won__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>false</editable>
        <field>Account.Total_Deals_Submitted__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <!-- Property_Registry__c.Protection_Window_Days__c ADDED 2026-08-30, BA Gap-Closure C2
         follow-up. Read-only, no matching <objectPermissions> block - same convention as every
         other object restored on this set (see the Portfolio_Deal__c note earlier in this file):
         this set is assigned only where the System Administrator profile's own Modify All Data
         already confers object-level access, so only the field-level grant is needed here.
         Exists so an administrator settling a GATE A dispute (the protection window in force
         when a claim was registered) can see the stamped value; PropertyClaimService.
         stampResolvedWindow is the sole writer, and needs Read+Edit only on
         Broker_Protection_Access, the intake principal - see that field's own description. -->
    <fieldPermissions>
        <editable>false</editable>
        <field>Property_Registry__c.Protection_Window_Days__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <hasActivationRequired>false</hasActivationRequired>
    <label>DPEG Admin Access</label>
    <!--
      DE-DUPLICATED 2026-08-14. The block that stood here declared SIX recordTypeVisibilities
      a second time - Account.Broker_Firm, Account.Investor_Entity, Contact.Broker,
      Contact.Investor, Lead.Acquisition_Broker, Lead.IR_Investor - all of which were already
      present in the sorted list below (and still are). It has been removed; each entry now
      appears exactly once, which is what the org holds.

      HOW IT GOT HERE, because the mistake is repeatable. The removed block carried a comment
      stating the six were "live on this set in usman-dpeg and missing from this file, so the
      next deploy of it would have destroyed them". The first half was true; the second was
      not. They were not missing - they were in the alphabetically sorted list further down,
      which is easy to miss because the appended copies sorted ABOVE it and so read as a
      separate, additional group. The 2026-08-10 reconciliation compared the ORG's entries
      against a scan of this file and appended what it believed was absent.

      THE GENERAL LESSON: when reconciling org -> repo, diff the SETS, not the presence of a
      block. A grep for a record type in a 400-line file that is sorted in one region and
      appended in another will answer "not found" for something that is present.

      MEASURED, not inferred: a retrieve of this set from usman-dpeg on 2026-08-14 returns
      16 recordTypeVisibilities, each exactly once; this file held 22, the six extras being
      precisely those duplicates.

      PRESERVED from the removed comment because it is a genuine, separate observation and is
      still true: the Account.* and Contact.* record types referenced below exist in the ORG
      but have no recordType metadata in this repo. That is why they cannot be verified by a
      file-based check, and it is unrelated to the duplication.

      Opportunity.Commercial WAS RETIRED AT PHASE 1 D4 and has been removed from this set;
      Opportunity.Retail replaced it. The two coexisted from D1 through the migration window on
      purpose. Do not re-add Commercial - the record type no longer exists and the deploy would
      fail. Sequence: agent-output/runbook-acquisition-observations-phase1.md.
    -->
    <recordTypeVisibilities>
        <recordType>Account.Broker_Firm</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Account.Investor_Entity</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Contact.Broker</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Contact.Investor</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Contract_Review__c.Acquisition_PSA</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Contract_Review__c.Disposition_PSA</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Disposition__c.Off_Market</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Disposition__c.On_Market</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>LOI__c.Acquisition_LOI</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>LOI__c.Disposition_LOI</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Lead.Acquisition_Broker</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Lead.IR_Investor</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>NDA__c.Acquisition_NDA</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>NDA__c.Disposition_NDA</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Opportunity.Land</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <recordTypeVisibilities>
        <recordType>Opportunity.Retail</recordType>
        <visible>true</visible>
    </recordTypeVisibilities>
    <tabSettings>
        <tab>Acquisition_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Active_Transactions</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Broker_Assignment__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Broker_Assignments</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Broker_Hub</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Broker_Scorecard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Construction_Feasibility_Review__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Contract_Review__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Development_Feasibility_Review__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Disposition_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Disposition__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>LOI__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lead_Funnel</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease_Activity_Tracker</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease_Inquiry__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease_Renewal__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease_Renewals</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Lease__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Leasing_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>NDA__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Offering__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Onboarding</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Onboarding__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Property_Asset__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Property_Management_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Property__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Sell_Meter</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Transaction_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Transaction__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Underwriting__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Work_Order_Dashboard</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Work_Order__c</tab>
        <visibility>Visible</visibility>
    </tabSettings>
    <tabSettings>
        <tab>Work_Orders</tab>
        <visibility>Visible</visibility>
    </tabSettings>
</PermissionSet>
